# Configuração do servidor Express (server/index.ts)
PORT=3001

# Caminho do ficheiro da base de dados SQLite (é criado automaticamente)
DB_PATH=data/boca-maldita.db

# URL pública onde a aplicação está alojada (usada para ligações canónicas e CORS em produção)
APP_URL="http://localhost:3000"